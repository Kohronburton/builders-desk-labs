# Contributing

1. Create a story or defect issue with acceptance criteria.
2. Branch from `main` using `demo/`, `feature/`, or `fix/`.
3. Keep commits focused and include tests with behavior changes.
4. Run `npm run ci` before opening a pull request.
5. Update the runbook and architecture when operational behavior changes.
6. Use squash merge for demo branches after review.
