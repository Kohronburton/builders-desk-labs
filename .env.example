# deterministic mode requires no external credentials
AI_MODE=deterministic

# live LLM mode
# AI_MODE=openai
# OPENAI_API_KEY=replace_me
OPENAI_MODEL=gpt-4o-mini

# override the n8n image when a pinned release is required
N8N_IMAGE=n8nio/n8n:latest
DIAGNOSTICS_TIMEOUT_MS=800
MOCK_TIMEOUT_MS=1200
