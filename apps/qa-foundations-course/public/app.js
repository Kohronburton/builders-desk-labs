const modules = [
 {title:'Workflow anatomy and the product spine',time:'30 min',outcome:'Map an n8n workflow as a testable business system, not a canvas of random nodes.',body:`<h3>What n8n executes</h3><p>A workflow begins with a trigger, passes data through one or more nodes, then ends in a business outcome: a record created, message sent, file exported, or API response returned. Manual Trigger is for controlled building and testing; production work needs an appropriate trigger and a published workflow.</p><h3>Map this before you build</h3><ol><li><strong>Command Center:</strong> owner, goal, trigger, business metric.</li><li><strong>Workspace:</strong> each node’s responsibility and input/output.</li><li><strong>Modules:</strong> intake, validation, enrichment, decision, action, reporting.</li><li><strong>Data Registry:</strong> fields, identifiers, source of truth, retention.</li><li><strong>Integrations:</strong> credentials, rate limits, retry behavior.</li><li><strong>System Health:</strong> error workflow, execution review, alerting.</li></ol>`,challenge:'Create a one-page workflow map for “lead intake → clean → AI score → approval → CRM export.” Name the trigger, every module, and the final business outcome.'},
 {title:'Items, JSON, and expressions',time:'40 min',outcome:'Trace exactly what each n8n node receives and returns—the core skill behind reliable workflows.',body:`<h3>The n8n data model</h3><p>Nodes pass a list of <strong>items</strong>. Each item normally has a <code>json</code> object. A Manual Trigger starts with no useful lead data, so use Edit Fields (Set) to make clean test items first.</p><pre>[{ "json": { "company_name": "Example Co", "website": "https://example.com" } }]</pre><h3>Expressions are data references</h3><p>Use <code>{{$json.company_name}}</code> for the current item. Reference a named node only when necessary: <code>{{$node["Lead Intake"].json["website"]}}</code>. Test each expression with realistic data. A missing node, a renamed field, or a value that is not an array can quietly break later logic.</p><h3>QA checks</h3><ul><li>One item versus many items—does the node run once per item?</li><li>Is a field missing, null, blank, number-as-text, or nested differently?</li><li>Does Merge preserve the correct pairing of items?</li><li>Are expressions evaluated with the current execution data, not stale pinned data?</li></ul>`,challenge:'Create three test items in Edit Fields. Use expressions to create a greeting and a normalized website field. Test blank website, two records, and a field renamed upstream.'},
 {title:'Triggers, schedules, and webhooks',time:'40 min',outcome:'Test how real events enter a workflow without accidentally treating test URLs as production.',body:`<h3>Choose the right trigger</h3><p>Manual Trigger is build-only. Schedule Trigger tests time zones, missed windows, and duplicate runs. App triggers test provider delivery. Webhook receives HTTP requests and can either respond immediately or return the workflow output.</p><h3>Webhook test contract</h3><ul><li>Use the <strong>test URL</strong> while listening in the editor; use the <strong>production URL</strong> only after publishing.</li><li>Verify method, path, required headers, authentication, and response code.</li><li>Send valid, missing-field, malformed JSON, unauthorized, duplicate, and replayed requests.</li><li>Decide what the caller gets if downstream work is slow: immediate acceptance or synchronous result.</li></ul><p>Every external webhook needs an idempotency strategy: store a source event ID or a deterministic dedupe key before creating irreversible records.</p>`,challenge:'Write a webhook contract for paid orders. Include expected payload, signature/auth check, response behavior, duplicate-event rule, and six test requests.'},
 {title:'HTTP Request and credentials',time:'40 min',outcome:'Call APIs safely and prove the integration handles real HTTP behavior.',body:`<h3>HTTP Request is your universal adapter</h3><p>Set method, URL, query values, headers, body, and authentication deliberately. Use Credentials for API keys, OAuth, basic auth, or custom headers—never paste secrets into Code, Edit Fields, or screenshots.</p><h3>Test beyond a successful GET</h3><ol><li>200/201: validate response shape and business effect.</li><li>400/422: validate your own input before calling the provider.</li><li>401/403: confirm expired or insufficient credentials fail safely.</li><li>404: confirm a missing resource does not create junk data.</li><li>429: observe rate-limit behavior and backoff plan.</li><li>500/timeouts: route to controlled recovery, never silently continue.</li></ol><p>For pagination, prove you collect all pages and stop correctly. For POST requests, attach an idempotency key where the provider supports it.</p>`,challenge:'Call a public API with HTTP Request. Record a test matrix for success, invalid query, unavailable endpoint, and rate-limit simulation. Store any real key only as an n8n credential.'},
 {title:'Flow logic and data transformations',time:'45 min',outcome:'Build deterministic decisions before adding AI or Code.',body:`<h3>Make business rules visible</h3><p>Use IF, Switch, Filter, Merge, Split Out, Loop Over Items, Aggregate, and Edit Fields to make the workflow readable. Prefer a small, obvious data node over a giant expression that nobody can debug.</p><h3>Common traps to test</h3><ul><li>IF comparisons where <code>"0"</code>, <code>0</code>, null, and empty string behave differently.</li><li>Merge modes that pair unrelated records after one branch filters items.</li><li>Split Out producing many child items but a later action expects one parent.</li><li>Loop retries creating duplicate emails or CRM records.</li><li>Code nodes returning invalid item format instead of <code>[{json:{...}}]</code>.</li></ul><p>Build a decision table first: input conditions, expected branch, action, and ownership of exceptions.</p>`,challenge:'Create a priority router: emergency, urgent, routine, and invalid. Test each category plus blank, lowercase/uppercase variants, and two items arriving together.'},
 {title:'Executions, pinning, and debugging',time:'35 min',outcome:'Use execution evidence to diagnose the exact node and data state that failed.',body:`<h3>Read executions as a timeline</h3><p>Inspect the trigger input, each node’s input/output, duration, retry attempts, and error message. Start with the first failed node; later nodes are often only symptoms.</p><h3>Pin and mock data correctly</h3><p>Pinning saves a node output so you can develop downstream nodes without calling an external service. It is excellent for repeatable debugging, but it can hide a changed API response. Unpin and test fresh data before release.</p><h3>Debug sequence</h3><ol><li>Reproduce using the same or sanitized input.</li><li>Compare expected versus actual item count and field shape.</li><li>Check expression previews and node parameters.</li><li>Confirm credential, provider status, and rate-limit facts.</li><li>Fix the earliest cause; rerun the complete path.</li></ol>`,challenge:'Pin a sample API response, build a downstream transform, then unpin it. Capture proof of the fresh run and write a two-sentence root-cause summary for one intentional failure.'},
 {title:'Error workflows, retries, and idempotency',time:'45 min',outcome:'Design a workflow that fails loudly, recovers safely, and never duplicates irreversible work.',body:`<h3>Failure is a workflow path</h3><p>Attach an error workflow starting with <strong>Error Trigger</strong>. Include execution ID, workflow name, failed node, sanitized error, and correlation/event ID in an alert or incident record.</p><h3>Retries must be deliberate</h3><p>Retry transient failures such as timeouts and 429s, preferably with delay/backoff. Do not blindly retry validation failures, auth failures, or a non-idempotent “send payment/email/create order” action.</p><h3>The idempotency pattern</h3><ol><li>Receive source event with event ID.</li><li>Look up event ID in a durable store.</li><li>If already complete, return a safe duplicate response.</li><li>If new, record “processing,” do the action, then record “complete.”</li><li>Route unknown/partial states to review.</li></ol>`,challenge:'Create an error-workflow design for a CRM export. Specify retryable errors, non-retryable errors, alert content, dedupe key, and human-review trigger.'},
 {title:'AI workflows and human review',time:'45 min',outcome:'Use AI where judgment helps while protecting deterministic business rules.',body:`<h3>AI output is untrusted input</h3><p>AI Agents can use models and tools, but they do not replace validation. Give the model focused context, ask for a structured result, validate required fields/types, then enforce deterministic rules before an irreversible action.</p><h3>Test an AI step like a risky integration</h3><ul><li>Representative good inputs, ambiguous inputs, malicious prompt text, empty input, and oversize input.</li><li>Schema failures, hallucinated fields, wrong classifications, unsafe tool calls, and provider timeouts.</li><li>Cost/latency limits and a fallback path.</li><li>Human approval for uncertain, high-impact, or externally visible outputs.</li></ul><p>Keep a small evaluation dataset with expected outcomes. Re-run it when changing prompts, models, tools, or upstream data.</p>`,challenge:'Design an AI lead-scoring output schema: score, reason, missing_data, recommended_action, confidence. Define when a human must approve instead of auto-exporting.'},
 {title:'Security, source control, and environments',time:'40 min',outcome:'Separate development from production and keep client data and credentials protected.',body:`<h3>Release safely</h3><p>Keep credentials in n8n’s credential system and restrict who can view or use them. Use separate development and production environments so experiments cannot break live workflows. Export/version workflows carefully and review changes before publishing.</p><h3>Security QA checklist</h3><ul><li>No keys, tokens, PII, or client payloads in Code, pinned data, screenshots, or Git.</li><li>Webhook authentication/signature verification is present and tested.</li><li>Least privilege for every provider credential.</li><li>Execution-data retention matches the business and privacy need.</li><li>Production workflow is published only after fresh end-to-end testing.</li></ul><p>Document rollback: what gets disabled, what data is preserved, and how to replay safely.</p>`,challenge:'Write a pre-release checklist for your workflow: credentials, test data cleanup, test URL vs production URL, error alert, publish step, monitoring owner, and rollback action.'},
 {title:'Deployment, scaling, and portfolio capstone',time:'60 min',outcome:'Deploy an n8n workflow responsibly and package proof that you can own a client automation.',body:`<h3>Production changes the engineering problem</h3><p>Self-hosted n8n needs persistent storage, encryption-key planning, environment configuration, backups, and observability. Queue mode separates the main process from workers for production execution scaling; webhooks still need reliable ingress and a visible failure path. Establish execution timeouts and decide what execution data is saved.</p><h3>Capstone: Lead Research → Cleanup → AI Review → Approval → Export</h3><ol><li>Webhook or schedule trigger with a documented contract.</li><li>Edit Fields/validation module and a data contract.</li><li>HTTP Request integration with credential-safe setup.</li><li>Deterministic cleanup and dedupe decision.</li><li>Structured AI recommendation with schema check and human approval.</li><li>Google Sheet/CRM export protected by idempotency.</li><li>Error workflow, alert, execution proof, release checklist.</li></ol>`,challenge:'Publish a portfolio QA packet: workflow map, 20-case test matrix, sample execution evidence, failure/retry design, security checklist, and a 3-minute demo script showing a success, an invalid request, and a controlled failure.'}
];
let current=Number(localStorage.getItem('bd-n8n-qa-current')||0),done=JSON.parse(localStorage.getItem('bd-n8n-qa-done')||'[]');const $=s=>document.querySelector(s);
function render(){const m=modules[current];$('#moduleNav').innerHTML=modules.map((x,i)=>`<button class="${i===current?'active':''}" data-module="${i}"><span>${String(i+1).padStart(2,'0')}</span>${x.title}${done.includes(i)?'<b>✓</b>':''}</button>`).join('');$('#moduleTag').textContent=`MODULE ${current+1}`;$('#lessonTime').textContent=m.time;$('#title').textContent=m.title;$('#outcome').textContent=m.outcome;$('#content').innerHTML=m.body;$('#challenge').innerHTML=`<p class="eyebrow">HANDS-ON ARTIFACT</p><strong>${m.challenge}</strong>`;$('#complete').checked=done.includes(current);$('#previous').disabled=current===0;$('#next').textContent=current===modules.length-1?'Finish course ✓':'Next module →';$('#progressText').textContent=`${done.length} / ${modules.length} modules`;$('#progressBar').style.width=`${done.length/modules.length*100}%`;localStorage.setItem('bd-n8n-qa-current',current)}
$('#moduleNav').addEventListener('click',e=>{const b=e.target.closest('button');if(b){current=Number(b.dataset.module);render()}});$('#complete').addEventListener('change',e=>{done=e.target.checked?[...new Set([...done,current])]:done.filter(i=>i!==current);localStorage.setItem('bd-n8n-qa-done',JSON.stringify(done));render()});$('#previous').onclick=()=>{current=Math.max(0,current-1);render()};$('#next').onclick=()=>{if(current<modules.length-1){current++;render()}else alert('Course complete. Build and share the n8n QA packet—this is the proof clients can review.')};$('#reset').onclick=()=>{if(confirm('Reset course progress?')){done=[];current=0;localStorage.removeItem('bd-n8n-qa-done');render()}};render();
