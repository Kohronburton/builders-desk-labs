services:
  - type: web
    name: builders-desk-catering-logistics
    runtime: python
    rootDir: apps/catering-logistics-claude
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
